- name: Download all packages from artifactory
      shell: >
        wget --user={{ url_username }} --password={{ url_password }} {{ url }}/eCMTS_{{ eCMTS_version }}/eCMTS-{{ eCMTS_version }}-1.x86_64.rpm -P {{ dest }}
      vars:
        url: "{{ artifactory_release_path }}"
        dest: /home/silver/
        url_username: "{{ artifactory_user }}"
        url_password: "{{ artifactory_password }}"

1.after the above step need to add the task to Check existing package installed on the server using the command rpm -qa | grep -i 'eCMTS'
2.print the eCMTS version
3.uninstall the  eCMTS  from server

please provide continuation code.

