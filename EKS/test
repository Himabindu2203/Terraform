- name: Validate eCMTS version
      shell: eCMTS --version
      register: ecmtsversion
      #become: yes
      #become_user: silver

    - name: Print the eCMTS version
      debug:
        msg: "{{ ecmtsversion.stdout }}"

error:

env: ecmts
operation: eCMTSversionupgrade
TARGET_HOST is set to ecmts_server
ERROR! 'shell' is not a valid attribute for a Play
The error appears to be in '/home/gitlab-runner/builds/VuRVMFqH/0/hmno-devops/wireline-infra-automation/scripts/eCMTSversionupgrade.yml': line 35, column 7, but may
be elsewhere in the file depending on the exact syntax problem.
The offending line appears to be:
    - name: Validate eCMTS version
      ^ here
