in the below script, 
#Setting temp profile for Properties automation, it'll help in case of vault properties updates
#Otherwise error can be thrown related to IAM profile and role mismatch
os.environ['AWS_PROFILE'] = "PROP_PROPERTIES_TEMP_PROFILE"
#file path for single file property update
prop_file_path = os.environ.get("file_path")
#To check for single property update


os.environ['AWS_PROFILE'] = "PROP_PROPERTIES_TEMP_PROFILE"  - in the scripts i coldnt find the file named with PROP_PROPERTIES_TEMP_PROFILE. how the values r pulling?
pls explin?
